import { HomeClient } from '@/components/HomeClient'

export const metadata = {
  alternates: { canonical: 'https://biyori-pilates.com/' },
}

export default function Home() {
  return <HomeClient />
}
